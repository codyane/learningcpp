#include <iostream>
#include <stdio.h>

int main ()
{
    // ternary operator ?: = replacement to an if/elses statement 
    // condition ? experssion1 : expression2;

/*
    int grade = 75;

    if(grade >=60)
    {
        std::cout << "You Pass!"; 
    }
    else{
        std::cout << "You Fail";
    }
*/

//int grade  = 70;
//grade >= 70 ? std::cout << "You Pass" : std::cout << "You fail";

//int number = 9;
//number / 2 ? std::cout << "ODD" : std:: cout << "EVEN";

//bool hungry = false;
//hungry ? std::cout << "You are hungry" : std::cout << "You are full";
// std::cout << (hungry ? "You are hungry" : "You are full ");

}